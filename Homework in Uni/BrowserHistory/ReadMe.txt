This task focuses on working with a structure and a class at the same time.

There is a structure:
-HistoryEntry it has 2 class members(int and char*), it is contained in the file Structures.h

There is one class:
-BrowserHistory it serves as HistoryEntry container. It can also find the most visited sites, edit entries and has a concat function